#!/usr/bin/env python3
"""Launch the complete assistant's guided installation and lifecycle commands."""
from pathlib import Path
import os
import sys

launcher = Path(__file__).resolve().parent / "hermes/hermes-pack"
os.execv(sys.executable, [sys.executable, str(launcher), *sys.argv[1:]])
